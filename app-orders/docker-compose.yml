## INTANCIAR PGSQL NO DOCKER COMPOSE
services:
  pg:
    image: bitnami/postgresql:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    volumes:
      - ./docker:/docker-entrypoint-initdb.d